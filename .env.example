# --- Google Sign-In (Continue with Google) ---
# Create credentials at https://console.cloud.google.com/apis/credentials (OAuth 2.0 Client ID, Web application).
# Add authorized JavaScript origins (e.g. http://localhost:5173) and use the Client ID below for both:
# VITE_GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
# GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com

# --- FREE: Gmail (no domain, no payment) ---
# Use a Gmail address + App Password so anyone can receive verification emails.
# 1. Turn on 2-Step Verification: https://myaccount.google.com/security
# 2. App passwords: https://myaccount.google.com/apppasswords → create one for "Mail"
# 3. Set below (use the 16-character app password, not your normal Gmail password):
# GMAIL_USER=yourname@gmail.com
# GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx

# --- Optional: Resend (requires verified domain to send to any address) ---
# RESEND_API_KEY=re_xxxxxxxxxxxx
# FROM_EMAIL=Gameratez <noreply@yourdomain.com>

# URL of your app (used in verification email links)
SITE_URL=http://localhost:5173

# Comma-separated list of allowed CORS origins (set to your Vercel URL in production)
# Example: CORS_ORIGINS=https://gameratez.vercel.app
# Defaults to SITE_URL if not set.
# CORS_ORIGINS=http://localhost:5173

# --- Database (REQUIRED before inviting users) ---
# Use a Postgres connection string from Render/Fly/Supabase/etc.
# DATABASE_URL=postgres://user:pass@host:5432/dbname
#
# If your provider requires SSL but uses a self-signed cert, keep default PGSSL behavior.
# Set PGSSL=false to disable SSL (local dev).
# PGSSL=false

# --- Admin moderation ---
# Used for admin-only endpoints like deleting reported rates.
# Send header: x-admin-token: <value>
# ADMIN_TOKEN=super-secret-token

# --- Production (Vercel frontend only) ---
# Set on Vercel so the frontend calls your deployed backend. No trailing slash.
# VITE_API_BASE_URL=https://your-backend.onrender.com
