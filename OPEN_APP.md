# Open Gameratez (localhost)

Run the app first: **`npm run dev`**

Then click this link to open the app in your browser:

**[Open app → http://localhost:5173](http://localhost:5173)**

Or run **`npm run open`** to open it automatically (macOS).
